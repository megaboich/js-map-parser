
/// <summary>
/// Function F1 - the main purpose is to illustrate XML comments working.
/// </summary>
/// <param name="originalComment">Parameter that is responsible for everything on this planet.</param>
/// <returns>Returns literally nothing, just to have a lot text in it.</returns>
function F1() {
    value.properties.push(new (acc ? Access : Index)(idx));
}

function F2() { }

function F3(def, classdef) {
    obj.prototype = new this(PROTO);
    return obj;
};

function F4() { }
